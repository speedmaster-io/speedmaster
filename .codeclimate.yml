---
engines:
  duplication:
    enabled: true
    config:
      languages:
        - php
  fixme:
    enabled: true
  phpcodesniffer:
    enabled: true
    config:
      standard: "WordPress-Core"
ratings:
  paths:
    - "**.php"
    - "wp-config.php"
exclude_paths:
  - tests/**/*
  - src/lib/**/*